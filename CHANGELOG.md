# Changelog

本项目用 Tauri v2 + React + Rust 完整重写
[henrypp/memreduct](https://github.com/henrypp/memreduct),
复刻其私有清理 API 与全部功能。所有版本均遵循 GPL-3.0。

## v3.5.14 (2026-09-15)

### 修复
- **检查更新可能永久卡住**：更新插件的 `UpdaterBuilder::timeout` 默认是 `None`，
  且其配置结构里**没有 timeout 字段**（写 `plugins.updater.timeout` 会被静默忽略），
  所以端点不可达时请求会一直挂着。现在检查 30 秒超时；下载另设 300 秒
  （插件构造 `Update` 时硬编码了 `timeout: None`，必须在下载前自己补）
- **代理拦截被误报成「未找到发布」**：插件对非 2xx 响应不记录错误，
  最终抛出 `ReleaseNotFound` —— 代理把 `github.com` 挡掉返回 502 时，
  用户看到的是"没有找到发布"，与真实原因无关。现在代理失败后会
  **忽略代理重试一次**，成功则下载也走同一条可用线路
- **检查更新失败完全不可见**：启动时的检查把错误全部 catch 掉，
  失败与"已是最新"在界面上完全一样。现在会记录并在「关于」页展示

### 优化
- 「关于」页新增更新诊断：上次检查结果（时间、正常/失败、**失败原因原文**）与
  **更新地址**（可点击在浏览器打开，是判断端点是否可达最快的方法）
- 新版本提示条带上更新说明的首条要点，不再只有版本号
- 移除非交互环境下的确认提示；手动检查的超时从 30s 调整为 35s，
  避免先于后端的 30s 超时误报

### 其它
- 移除 `tauri.conf.json` 中 `plugins.updater.active` —— 该字段并不存在，
  留着会让人误以为它是更新开关。更新地址由 `updater.rs` 在运行时按架构拼出
- 新增标准化发版脚本 `scripts/release.mjs`（见 CONTRIBUTING）
- 后端单元测试 31 → 35

## v3.5.13 (2026-09-15)

> 版本号已同步至 `package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、
> `src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`。

### 修复
- **重复启动出现多个托盘图标**:新增单实例保护(`Local\` 命名互斥 + 等待语义),
  第二次启动不再新建托盘/后台循环,而是唤起已有窗口并退出。
  提权重启(手动清理触发 UAC)通过 `-takeover` 标记交接,不会误判为“已在运行”而丢失窗口
- **全局热键未加 `MOD_NOREPEAT`**:长按热键会按系统重复频率连续清理;
  热键注册失败原先被静默吞掉(界面开关仍显示已启用),现在会向前端上报并提示用户
- **主界面勾选的清理区域不保存**:托盘菜单、全局热键、自动清理一直在用配置里的旧掩码,
  重启后选择还会丢失;现在勾选即写回 `reduct_mask`
- **配置值未校验**:阈值/间隔为 0 会让“按间隔清理”判断(`elapsed >= 0`)恒真、
  每 30 秒狂清理一次;警告/危险级别交叉会让托盘警告色失效。
  新增 `Config::sanitize()`(加载、保存、命令入参三处统一夹紧),
  并校验 `theme`/`accent_color`/`language` 白名单、掩码与托盘动作取值
- **卷缓存刷新可能卡死界面**:原先盲目遍历 `\\?\A:` … `\\?\Z:`,
  遇到光驱/断连的映射网络盘会阻塞调用线程;
  改为 `GetLogicalDrives` + `GetDriveTypeW` 只处理真实存在的本地卷
- **清理失败无任何反馈**:所有 `NtSetSystemInformation` 返回码都被丢弃,
  失败时只显示“释放了 0 B”。现在会收集失败区域并在提示中给出“N 个失败”,
  CLI 在**全部**区域失败时返回退出码 1
- `Mutex::lock().unwrap()` + `panic = "abort"`:任意线程在持锁时 panic 会让后续
  取锁直接终止整个进程(含托盘),改为中毒恢复取锁
- 托盘单击/双击冲突:双击动作设为“清理内存”时,系统仍会先投递一次单击事件导致窗口被弹出;
  现在单击动作按系统双击间隔延迟并在检测到双击时取消
- Windows 上 `TrayIcon::set_title` 是空实现(tray-icon 0.24),移除相关死代码,
  百分比只通过图标位图和 tooltip 传达
- `flush_volume_cache` 中 `-startup` 之外,提权重启还会剔除已有的 `-takeover`
- 页文件结构体遍历中冗余的 `first` 分支
- en-US 语言包 `status.warn` 误译为 "Elevated"(应为 "High")

### 优化
- 清理入口统一:界面按钮 / 托盘菜单 / 全局热键 / 自动清理共用一条
  `perform_clean` 路径,统计时间戳、落盘与 `memory-update` 事件不再各写各的
  (托盘与热键此前完全不做这两件事),并新增 `clean-done` 事件让托盘/热键/自动清理
  也能在界面上给出反馈
- 系统能力(8.1+ / 10+)只查询一次(`OnceLock`),不再每次清理重复调 `RtlGetVersion`
- 物理内存阈值:主界面状态徽章、仪表环与迷你进度条改用用户配置的
  警告/危险级别(原先硬编码 70/90,与托盘设置不一致)
- 当前系统不支持的区域(注册表缓存 / 合并内存列表)在界面上禁用并说明原因
- 区域开关改为真正的 `role="checkbox"` 按钮(原先 `<label onClick>` 无法用键盘操作、
  读屏不可见),补充 `:focus-visible` 样式;确认弹窗支持 Esc 关闭
- 清理区域掩码顺序在前后端统一(后端 `mask::names()` 与 `regions.ts` 顺序一致)
- 更新清单地址按编译架构生成(`x86_64` / `aarch64` / `i686`),不再硬编码 x86_64
- 移除死代码:`CleanResult.elevation_requested`、`config::color_to_hex`、
  `regions.ts` 的 `maskNames`/`MASK_FREEZES`、`api.ts` 中后端并不存在的
  `tray_use_antialiasing`;抽出共用的 `shell_open()`
- 更新检查/托盘/设置面板文案改为 `i18n.t`,不再因挂载时闭包捕获而语言滞后
- 关闭窗口首次弹出“仍在托盘运行”提示(新增配置 `tray_tip_shown`)

### 工具链
- `scripts/verify_icon_png.mjs` 原先只打印像素、从不断言,现改为真校验(空白图标退出码 1),
  并在 CI 中执行
- `scripts/extract_ico_frame.py` 硬编码 `/tmp/ico_frame.png`,在 Windows 上直接崩溃,
  改用系统临时目录并支持 `argv[1]`
- 新增 `scripts/README.md`,说明图标存在三条互相竞争、产物同名却颜色不同的链路,
  标注当前有效链路(`render_from_png.mjs` → `make_ico_bmp.py`)与已废弃链路
- 修正 `README.md` 中“`assets/icon.svg` 为唯一源”的错误描述 ——
  实际发行图标为蓝色 `#3366FF`,来自 `assets/icon-reference.png`,
  紫色 `icon.svg` 并未参与构建

### 测试
- 后端单元测试 22 → 31 个:新增配置夹紧、自动清理 0 值防御、热键编码/无效组合、
  掩码顺序一致性、单实例获取与释放等用例

## v3.5.12 (2026-09-14)

### 修复
- **全局热键泄漏**:重新设置热键时,旧监听线程阻塞在 `GetMessageW` 上永不退出,
  旧热键因此一直生效且线程持续泄漏 —— 改用 `PeekMessage` 轮询,并在注册新热键前
  `join` 旧线程,保证同一时刻只有一个注册
- **命令行清理**:`-clean` / `-clean:full` / `-clean-once <mask>` 现在真正执行清理后
  直接退出、不再弹出主窗口;未提权时自动请求 UAC 并以同参数重新执行
- **手动清理提权重启后窗口消失**:重启的实例不再继承 `-startup`,窗口保持显示
- **主题颜色失效**:前端写入的变量名与 CSS 不一致(`--accent2` vs `--accent-2`),
  且深色模式硬编码覆盖 `--accent`、软色/光晕恒为绿色 —— 现在只注入 `--accent-base`,
  其余色调全部由 CSS 派生,浅色/深色都正确跟随
- **语言选择器**:下拉框列出 7 种语言却只有 4 套翻译,选韩/德/法语会静默显示中文 ——
  只列出实际提供的语言,并按系统语言正确识别 zh-TW / ja-JP
- 滑块/开关每次变化都写盘并重启热键线程 → 改为 300ms 合并保存,且仅在热键真正变化时重注册
- 前端每秒既轮询又接收后端推送(双倍 IPC)→ 只保留后端事件
- 托盘图标每秒无条件重绘 → 仅在百分比/样式变化时重绘
- 页文件统计在异常样本下可能整数下溢 panic → 改用 `saturating_sub`
- 配置文件改为「写临时文件后重命名」,中断写入不再损坏配置

### 新增
- 托盘菜单跟随应用语言(原先后端硬编码中文)
- 主界面显示权限徽章(管理员 / 普通权限),普通权限时悬浮提示如何永久提权
- 「启动时最小化到托盘」开关(配置项原先存在但无法设置)
- 窗口默认隐藏创建、按需显示,消除启动瞬间的闪烁
- 「关于」页显示当前生效的配置文件位置(便携 / 应用数据)

### 优化
- 移除无效配置项(窗口置顶、通知声音、清理日志、托盘字体/抗锯齿、更新源与公钥)
- 移除重复的图标素材(`清理.png`、`assets/清理.svg`)并修正图标生成脚本路径
- `cargo fmt` / `cargo clippy --all-targets -D warnings` / `tsc` / `cargo test` 全绿
  (新增 6 个单元测试)

## v3.5.3 (2026-08-24)

### 修复
- 修复物理内存占用始终显示 0% 的问题 — 改用 `GlobalMemoryStatusEx` 采集
  物理内存(原字节缓冲读取 `SystemPerformanceInformation` 在缓冲不足时失败)
- 修复 release 版 UAC 通过后应用静默崩溃的问题 — 补充 `plugins.updater`
  配置,修复托盘图标/页文件解析的潜在 panic(panic=abort 会静默退出)
- 修复设置面板卡片内容边距不足的问题
- 修复通知条每条消息显示两条的问题
- 修复打开设置界面闪烁(主/设置视图常驻不卸载,改为 display 切换)

### 新增
- **管理员权限(复刻原项目 UAC 流程)**:启动普通权限不弹UAC;手动清理
  未提权时弹一次 UAC 将整个应用重启为管理员进程(原版 `_r_app_runasadmin`
  的 runas 重启),之后所有清理免UAC;开机静默自启用计划任务登录时最高
  权限静默提权
- **自动更新简化**:启动自动检查更新并 toast 提示;单个「检查更新」按钮;
  发现新版自动下载+静默安装+重启应用;设置面板显示当前版本;
  移除仓库/密钥配置与「自动检查」开关
- **移除窗口置顶功能**
- **主题切换**:浅色 / 深色 / 跟随系统三态(外观设置)
- **主题颜色预设**:绿/紫/蓝/橙/红/青/粉 7 个预设色(外观设置色块)
- 多语言支持 (i18n): 简体中文(主语言)、繁體中文、English、日本語
- 全新应用图标 (紫色清理主题 SVG → PNG/ICO)
- 自动更新检查 (tauri-plugin-updater)
- 系统通知 (tauri-plugin-notification)
- 托盘百分比图标动态渲染 (颜色随警告/危险阈值变化)

### 优化
- 界面排版全面重构: 统一视觉体系、状态色与动效
- 清理确认对话框
- 托盘右键菜单 (显示窗口/清理内存/设置/官方网站/关于/退出)
- 全局热键动态注册/注销
- 代码质量: `cargo fmt` / `cargo clippy -D warnings` 全绿

## 核心功能(自初版)

- 8 区域内存清理: 工作集 / 系统文件缓存 / 修改文件缓存 / 修改页列表 /
  Standby 列表 / Standby 优先级0 / 注册表缓存 / 合并内存列表
- 内存监控: 物理内存 / 页面文件 / 系统缓存
- 自动清理 (阈值 / 间隔,30s 冷却)
- 全局热键 (默认 Ctrl+F1)
- 命令行: `-clean` / `-clean:full`
- portable / appdata 双模式配置存储
- dark/light 主题
